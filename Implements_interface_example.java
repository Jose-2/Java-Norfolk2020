package interface_example;

class Implements_interface_example implements An_interface_example{//implementing the interface allows the methods within the interface to be inherited
	public void method(int number) {
		System.out.println("A implemented number"+ (number));
		
	}

}
